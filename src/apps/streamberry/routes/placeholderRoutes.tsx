import type { RouteObject } from 'react-router-dom';

/**
 * All primary routes (home, library, search, detail, video) are real
 * implementations. This array is kept as an extension point but is empty.
 *
 * TODO: Delete this file once confirmed nothing imports it.
 */
export const STREAMBERRY_REMAINING_PLACEHOLDER_ROUTES: RouteObject[] = [];
